<Window x:Class="TelHai.DotNet.PlayerProject.AddSongWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Song Details" Height="260" Width="380"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Input fields -->
        <StackPanel>
            <TextBlock Text="Artist:"/>
            <TextBox x:Name="txtArtist" Margin="0,5,0,10"/>

            <TextBlock Text="Title:"/>
            <TextBox x:Name="txtTitle" Margin="0,5,0,10"/>

            <TextBlock Text="Duration (minutes):"/>
            <TextBox x:Name="txtDuration" Margin="0,5,0,10"/>
        </StackPanel>

        <!-- Buttons -->
        <Grid Grid.Row="3" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="OK" 
                    Height="35" Width="120" 
                    Background="#007ACC" Foreground="White"
                    HorizontalAlignment="Center"
                    Margin="5"
                    Click="BtnOk_Click"/>

            <Button Grid.Column="1" Content="Cancel"
                    Height="35" Width="120"
                    Background="#555" Foreground="White"
                    HorizontalAlignment="Center"
                    Margin="5"
                    Click="BtnCancel_Click"/>
        </Grid>

    </Grid>
</Window>
