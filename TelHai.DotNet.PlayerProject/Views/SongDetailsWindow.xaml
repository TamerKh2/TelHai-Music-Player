<Window x:Class="TelHai.DotNet.PlayerProject.Views.SongDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Song Details (MVVM)" Height="520" Width="820"
        Background="#222" Foreground="White"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="{Binding HeaderText}"
                   FontSize="18" FontWeight="Bold"
                   Margin="0,0,0,10"/>

        <!-- Search bar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Search iTunes:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox Width="260" Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,8,0"/>
            <Button Content="Search" Width="100" Command="{Binding SearchCommand}" Margin="0,0,8,0"/>
            <Button Content="Cancel" Width="100" Command="{Binding CancelCommand}" />
        </StackPanel>

        <!-- Results placeholder (we’ll improve next step) -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Text="{Binding StatusText}" Foreground="#AAA" Margin="0,0,0,8"/>

            <ListBox Grid.Row="1"
                     ItemsSource="{Binding Results}"
                     SelectedItem="{Binding SelectedResult}"
                     Background="#333"
                     Foreground="White"/>
        </Grid>
    </Grid>
</Window>
